[package]
name = "request-eagle"
version = "0.1.7"
edition = "2024"

[features]
dev-profiler = ["dep:gpui-profiler"]

[dependencies]
collection = { path = "../collection" }
preferences = { path = "../preferences" }
gpui-kit = { workspace = true, features = ["component", "assets"] }
# The kit does not forward GPUI's profiler feature. Enable it on the same
# pinned GPUI package; its overlay API is still imported through gpui_kit.
gpui-profiler = { package = "gpui-pre", version = "=0.3.1", default-features = false, features = ["profiler"], optional = true }
keybindings_service = { path = "../keybindings_service" }
reqwest_client.workspace = true
updater = { path = "../updater" }
request-eagle-theme = { package = "theme", path = "../theme" }
workspace = { package = "workspace", path = "../workspace" }

[target.'cfg(target_os = "macos")'.dependencies]
objc2-app-kit = { version = "0.3.2", default-features = false, features = ["NSAccessibility", "NSWorkspace"] }

[dev-dependencies]
gpui-kit = { workspace = true, features = ["test-support"] }
