[package]
name = "keybindings_service"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
gpui.workspace = true
thiserror.workspace = true
serde_json = "1"
tempfile = "3"

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }
