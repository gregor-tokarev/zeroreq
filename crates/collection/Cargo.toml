[package]
name = "collection"
version = "0.1.0"
edition = "2024"

[dependencies]
environment = { path = "../environment" }
serde = { version = "1", features = ["derive"] }
thiserror.workspace = true
toml = "1.1.3"
toml_edit = "0.25.13"
